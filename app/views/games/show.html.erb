<%= hidden_field_tag :game_id, @game.id %>

<% s = @game.state %>
<% e = 0 %>
<table id="game-grid" data-game-id="<%= @game.id %>">
  <% (1..3).each do |r| %>
    <tr>
      <% (1..3).each do |c| %>
        <%= render partial: "games/cell", locals: { xo: s[e], id: e } %>
        <% e += 1 %>
      <% end %>
    </tr>
  <% end %>
</table>

